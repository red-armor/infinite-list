import { metroInfiniteListResolver } from './metro-infinite-list-resolver';

describe('metroInfiniteListResolver', () => {
  it('should work', () => {
    expect(metroInfiniteListResolver()).toEqual('metro-infinite-list-resolver');
  });
});
