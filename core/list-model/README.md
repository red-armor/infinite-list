# list-model

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build list-model` to build the library.

## Running unit tests

Run `nx test list-model` to execute the unit tests via [Vitest](https://vitest.dev/).
