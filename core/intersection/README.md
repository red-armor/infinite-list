# intersection

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build intersection` to build the library.

## Running unit tests

Run `nx test intersection` to execute the unit tests via [Vitest](https://vitest.dev/).
