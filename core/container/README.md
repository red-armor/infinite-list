# container

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build container` to build the library.

## Running unit tests

Run `nx test container` to execute the unit tests via [Vitest](https://vitest.dev/).
