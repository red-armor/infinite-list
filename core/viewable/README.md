# viewable

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build viewable` to build the library.

## Running unit tests

Run `nx test viewable` to execute the unit tests via [Vitest](https://vitest.dev/).
