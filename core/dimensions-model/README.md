# dimensions-model

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dimensions-model` to build the library.

## Running unit tests

Run `nx test dimensions-model` to execute the unit tests via [Vitest](https://vitest.dev/).
