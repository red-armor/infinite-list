# disposable

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build disposable` to build the library.

## Running unit tests

Run `nx test disposable` to execute the unit tests via [Vitest](https://vitest.dev/).
