{
  "name": "@infinite-list/data-model",
  "version": "0.0.1",
  "files": [
    "dist",
    "src"
  ],
  "main": "src/index.ts",
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js"
  },
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsup",
    "test": "vitest"
  },
  "devDependencies": {
    "rimraf": "~4.4.0"
  },
  "dependencies": {
    "@x-oasis/batchinator": "^0.0.6",
    "@x-oasis/capitalize": "^0.0.6",
    "@x-oasis/noop": "^0.0.6",
    "@x-oasis/resolve-changed": "^0.0.6",
    "@x-oasis/shallow-array-equal": "^0.0.6",
    "@x-oasis/shallow-equal": "^0.0.6"
  }
}
