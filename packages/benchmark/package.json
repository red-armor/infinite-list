{
  "name": "@infinite-list/benchmark",
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsup"
  },
  "devDependencies": {
    "rimraf": "~4.4.0"
  }
}
