# group-dimensions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build group-dimensions` to build the library.

## Running unit tests

Run `nx test group-dimensions` to execute the unit tests via [Vitest](https://vitest.dev/).
