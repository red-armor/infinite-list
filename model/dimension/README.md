# dimension

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dimension` to build the library.

## Running unit tests

Run `nx test dimension` to execute the unit tests via [Vitest](https://vitest.dev/).
