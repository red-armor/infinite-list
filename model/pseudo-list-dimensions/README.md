# pseudo-list-dimensions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pseudo-list-dimensions` to build the library.

## Running unit tests

Run `nx test pseudo-list-dimensions` to execute the unit tests via [Vitest](https://vitest.dev/).
