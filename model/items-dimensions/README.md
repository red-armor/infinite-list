# items-dimensions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build items-dimensions` to build the library.

## Running unit tests

Run `nx test items-dimensions` to execute the unit tests via [Vitest](https://vitest.dev/).
